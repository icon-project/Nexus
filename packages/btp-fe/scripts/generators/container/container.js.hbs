import React from 'react';
{{#if wantTranslate}}
import { useTranslation } from "react-i18next";
{{/if}}
{{#if wantModel}}
import { addStoreModel } from 'utils/addStoreModel';
import { useDispatch, useSelector } from 'hooks';
import {{camelCase name}}Model from './{{camelCase name}}Model';

addStoreModel({{camelCase name}}Model);
{{/if}}

const {{properCase name}} = () => {
  {{#if wantTranslate}}
  // eslint-disable-next-line no-unused-vars
  const { t } = useTranslation();
  {{/if}}
  {{#if wantModel}}
  // eslint-disable-next-line no-unused-vars
  const dispatch = useDispatch(d => d.{{camelCase name}}Model);
  // eslint-disable-next-line no-unused-vars
  const state = useSelector(s => s.{{camelCase name}}Model);
  {{/if}}

  return <div></div>;
};

export default {{properCase name}};
