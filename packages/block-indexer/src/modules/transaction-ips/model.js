/* eslint-disable quotes */
'use strict';
const { getTransactionIP, updateTransactionIP, createTransactionIP } = require('./repository');

module.exports = {
  getTransactionIP,
  updateTransactionIP,
  createTransactionIP
};
